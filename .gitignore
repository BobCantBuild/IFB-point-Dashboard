# SQLite — all DBs excluded from tracking
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Local data files — generated at runtime, not needed in git
data/api_data.json
data/followups.json
IFB_Point_Master.txt

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.Python
build/
dist/

# Virtual environments (uv creates .venv — do NOT commit it)
.venv/
venv/
env/
ENV/

# uv — lock file IS committed (reproducible installs), venv is not
# uv.lock  ← committed intentionally, do not add here

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Streamlit
.streamlit/secrets.toml

# Logs / temp
*.log
*.tmp
run_app.sh*
backup*
.hermes

